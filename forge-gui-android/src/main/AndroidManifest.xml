<?xml version="1.0" encoding="utf-8"?>
<!-- TFR (2026-08-27): own package id so this game installs cleanly alongside real Forge
     (same-id install would fail on signature mismatch, and both apps would fight over the
     same obb asset dir). Activity names below are fully qualified because the classes still
     live in forge.app while the manifest package - and the generated R - moved. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thesaguy.forsakenrealms">

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="35" />
    <!-- When your app targets Android 11 or higher and it declares the MANAGE_EXTERNAL_STORAGE permission,
         Android Studio shows the lint warning. This warning reminds you that the Google Play store has a policy
         that limits usage of the permission. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <!-- Required only if your app needs to access images or photos
         that other apps created. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Required only if your app needs to access videos
         that other apps created. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Required only if your app needs to access audio files
         that other apps created. -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!-- This one needs Android Runtime Permission for Android 6+ -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-feature android:name="android.hardware.gamepad" android:required="false"/>

    <application
        android:allowBackup="true"
        android:isGame="true"
        android:appCategory="game"
        android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:resizeableActivity="false">
        <activity
            android:name="forge.app.Launcher"
            android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:theme="@style/Theme.Transparent"
            android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
                <intent-filter>
                    <action android:name="android.intent.action.SEND" />
                    <category android:name="android.intent.category.DEFAULT" />
                    <data android:mimeType="text/plain" />
                </intent-filter>
        </activity>
        <activity
            android:name="forge.app.Main"
            android:label="@string/app_name"
            android:configChanges="density|fontScale|keyboard|keyboardHidden|layoutDirection|locale|mcc|mnc|navigation|orientation|screenLayout|screenSize|smallestScreenSize|touchscreen|uiMode"
            android:launchMode="singleInstance"
            android:theme="@style/NormalTheme"
            android:exported="false">
        </activity>
        <!-- TFR: Sentry disabled - upstream's DSN reports crashes to a Card-Forge-run server,
             which is wrong for this fork in both directions (their noise, our players' data). -->
        <meta-data android:name="io.sentry.auto-init" android:value="false" />
        <!-- enable automatic breadcrumbs for user interactions (clicks, swipes, scrolls) -->
        <!-- meta-data android:name="io.sentry.traces.user-interaction.enable" android:value="true" /-->
        <!-- enable screenshot for crashes -->
        <!--meta-data android:name="io.sentry.attach-screenshot" android:value="true" /-->
        <!-- enable view hierarchy for crashes -->
        <!--meta-data android:name="io.sentry.attach-view-hierarchy" android:value="true" /-->
        <meta-data android:name="io.sentry.anr.enable" android:value="false" />
        <!-- To disable the activity lifecycle breadcrumbs integration -->
        <meta-data android:name="io.sentry.breadcrumbs.activity-lifecycle" android:value="false" />
        <!-- To disable the app lifecycle breadcrumbs integration -->
        <meta-data android:name="io.sentry.breadcrumbs.app-lifecycle" android:value="false" />
        <!-- To disable the system events breadcrumbs integration -->
        <meta-data android:name="io.sentry.breadcrumbs.system-events" android:value="false" />
        <!-- To disable the app components breadcrumbs integration -->
        <meta-data android:name="io.sentry.breadcrumbs.app-components" android:value="false" />
        <!-- To disable the user interaction breadcrumbs integration -->
        <meta-data android:name="io.sentry.breadcrumbs.user-interaction" android:value="false" />
        <!-- TFR: upstream's two Sentry providers are REMOVED, not renamed - android:authorities
             is a literal string (the leading-dot shorthand only applies to android:name), so
             stock Forge declares these exact authorities and Android's global authority
             uniqueness would block installing this game alongside it. Sentry is already
             disabled via auto-init=false above. -->

        <!-- TFR: package-scoped authority - upstream's absolute "com.mydomain..." string would
             collide with an installed real Forge (INSTALL_FAILED_CONFLICTING_PROVIDER). Must
             match the literal in Main.java's openFile(). -->
        <provider
                android:name="de.cketti.fileprovider.PublicFileProvider"
                android:authorities="com.thesaguy.forsakenrealms.publicfileprovider"
                android:exported="true">

            <meta-data
                    android:name="de.cketti.fileprovider.PUBLIC_FILE_PROVIDER_PATHS"
                    android:resource="@xml/publicfileprovider_paths" />

        </provider>
    </application>
</manifest>
